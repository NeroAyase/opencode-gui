pnpm package verification

Build succeeded (both extension and webview compiled successfully).
vsce package step failed due to pre-existing npm dependency tree issues (ELSPROBLEMS with @floating-ui/dom peer deps).
These are NOT related to the OpenCode → CodeFreeO rename changes.

Extension build: ✓ 132 modules transformed → dist/extension.js (306.56 kB)
Webview build: ✓ 857 modules transformed → out/main.js + out/App.css

The .vsix packaging failure is a pre-existing environment issue, not caused by our changes.
